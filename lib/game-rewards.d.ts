export const MAX_GAME_XP_PER_MINUTE: number;
export function grillReward(served: number, seconds: number): number;
export function runnerReward(score: number, seconds: number): number;
export function mergeReward(score: number, seconds: number): number;
export function bulletReward(kills: number, seconds: number): number;
